let companyProfile = require("./users/companyProfile");
let printerConfigurations = require("./settings/printerConfigurations");
let users = require("./users/user");
let roles = require("./users/role");

export const service = {
  companyProfile,
  printerConfigurations,
  users,
  roles
};
